# EXAMPLE ONLY — do not commit real secrets.
# Create secrets via CLI:
#   kubectl create secret generic viz-agent-secrets \
#     --from-literal=OPENAI_API_KEY="${OPENAI_API_KEY}" \
#     --from-literal=DATABASE_URL="${DATABASE_URL}" \
#     --from-literal=REDIS_URL="${REDIS_URL}"
apiVersion: v1
kind: Secret
metadata:
  name: viz-agent-secrets
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_SECRET"
  SENTRY_DSN: ""
  DATABASE_URL: "postgresql://user:pass@db:5432/vizdb"
  REDIS_URL: "redis://:pass@redis:6379/0"
